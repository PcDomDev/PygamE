"""Small, reusable gameplay Components used by the example scene
(examples/example_scene.py) - ResetOnClick, JumpsHUD. Not engine code:
these are ordinary Components written the same way any of your own
gameplay scripts would be, just kept separate from example_scene.py so
each is independently reusable/readable.
"""
