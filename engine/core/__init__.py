"""Engine core: the window/loop (App/Engine), the GameObject/Scene/
SceneManager machinery, runtime diagnostics (DebugManager), and the
SpatialHash collision broad-phase index. Everything a game built on this
engine touches indirectly through Scene/GameObject, rather than directly.
"""
